package com.cbslprojects.crm.CRM.Interfaces;

import android.support.v4.app.Fragment;

public interface LogoutAndShowSearcViewListener {
    void logout();
    void showSearchView(int fragmentId, Fragment fragment);
}
